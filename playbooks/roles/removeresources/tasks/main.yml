---
- include: acls.yml

- include: phppools.yml

- include: letsencrypt.yml

- include: vhosts.yml

- include: logrotate.yml

- include: "solr-shared-debian{{ ansible_distribution_major_version }}.yml"
  when: solr_required == True

- include: "solr-cores-debian{{ ansible_distribution_major_version }}.yml"
  when: solr_required == True

- name: flush handlers before we remove the basic resources
  meta: flush_handlers

- include: databases.yml

- include: symlinks.yml

- include: directories.yml

- include: useraccess.yml

- include: users.yml
